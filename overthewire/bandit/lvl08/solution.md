Use the `grep` command, whose `man` page can be found here: [grep — search a file for a pattern](https://man7.org/linux/man-pages/man1/grep.1p.html)  
The synopsis shows you can add a pattern list, then the filename.

```bash
grep “millionth” data.txt
```
