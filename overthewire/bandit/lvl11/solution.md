Use the `base64` command to base64 encode/decode data and print to standard output and upon checking the manual ([base64(1) - Linux man page](https://linux.die.net/man/1/base64), you will see the `-d` flag, which decodes data, and the snyopsis. 
```bash
base64 -d data.txt
```
