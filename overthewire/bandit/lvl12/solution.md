Check the “tr” manual and you will see in the synopsis that you need to add 2 strings after the option, and the first char of string1 will be translated to the first char of string2 and so on. But if you add the name of the file in tr, it won’t work, so you have to mention it before and create a pipe so that the computer understand you translating that file. “cat data.txt | tr “a-zA-Z” “n-za-mN-ZA-M”” will give you the password because the output of opening it is the file content, which you translate using rot13 logic, so a translates to n etc. but if j=you just put “n-m”, it will say “range-endpoints of 'n-m' are in reverse collating sequence order” so that’s why you add the z so it understand you taking the alphabet from the start again. It’s like adding 13 to 8 and the computer sees 11, but you explain to it that you carry 1, so it’s 21.
