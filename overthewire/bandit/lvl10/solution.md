Use the `strings` command, which prints the sequences of printable characters in files (the `man` page: [strings(1) — Linux manual page](https://man7.org/linux/man-pages/man1/strings.1.html))
```bash
strings data.txt | grep “=“
```
