It is recommended to read all the information provided in the links provided by the challenge and official documentation, such as the manual pages: [git - the content tracker](https://git-scm.com/docs/git).  
Upon checking the git-clone synoposis (found in the following manualpage: [git-clone - Clone a repository into a new directory](https://git-scm.com/docs/git-clone)), the following commands can be run:
```bash
git clone ssh://bandit27-git@bandit.labs.overthewire.org/home/bandit27-git/repo
ls
#a directory called "repo" can be seen

cd repo
ls
#a file called "README" can be seen

cat README
```
